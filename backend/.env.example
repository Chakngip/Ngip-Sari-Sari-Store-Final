# Server
PORT=5001
NODE_ENV=development

# Database (PostgreSQL)
DB_NAME=ngip_store
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=127.0.0.1
DB_PORT=5432

# Auth
JWT_SECRET=change_this_to_a_long_random_string
JWT_EXPIRES_IN=8h
